# docs — 项目文档

本目录收纳跨模块共享文档、接口契约、数据 schema 和后续设计说明。模块内部说明仍保留在各自模块目录下。

## 文档清单

| 文档 | 说明 |
|------|------|
| [REDIS_SCHEMA.md](REDIS_SCHEMA.md) | Redis 实时数据 key、字段、类型、TTL 和读写契约 |

## 维护约定

- 跨模块共享的规范文档放在 `docs/`。
- 模块内部运行、部署、开发说明放在对应模块 README。
- 文档内引用跨模块文件时，优先使用相对路径，避免移动后链接失效。