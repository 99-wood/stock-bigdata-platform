server:
  port: 8088

spring:
  application:
    name: stock-api
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

platform:
  push:
    interval: 5000

logging:
  level:
    com.stock.api: INFO
